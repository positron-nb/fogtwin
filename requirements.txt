fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27          # sim/fleet_sim.py only
paho-mqtt>=2.0       # optional: enable with FOGTWIN_MQTT=<broker host>

# numpy/pillow moved to requirements-build.txt: they're only needed to
# regenerate the terrain and road network from the DEM. The committed outputs
# in data/ mean the deployed server never touches those scripts, and skipping
# them keeps the runtime install small — this is what a host actually installs.
