fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27          # sim/fleet_sim.py only
paho-mqtt>=2.0       # optional: enable with FOGTWIN_MQTT=<broker host>

# Build-time only. Needed to regenerate the terrain and road network from the
# DEM (scripts/fetch_dem.py, scripts/build_roadgraph.py). The committed outputs
# in data/ mean the server and both front ends run without these.
numpy>=1.26
pillow>=10.0
